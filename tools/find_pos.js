const fs=require('fs');const path=require('path');const p=path.join(process.cwd(),'orchestration-builder.html');const s=fs.readFileSync(p,'utf8');const re=/<script[^>]*>([\s\S]*?)<\/script>/i;const m=re.exec(s);if(!m){console.error('no script');process.exit(1);}const script=m[1];const pos=90457;let line=1;let col=1;for(let i=0;i<pos && i<script.length;i++){ if(script[i]=='\n'){ line++; col=1;} else col++; }console.log('line in script:',line,'col:',col);const lines=script.split('\n');const start=Math.max(0,line-5);const end=Math.min(lines.length,line+5);for(let i=start;i<end;i++){ const ln=i+1; console.log((ln===line? '>>':'  ')+ln+': '+lines[i]); }