module.exports = {
  // Set the AI endpoint used by the server and frontends when stored in the repo
  AI_ENDPOINT: 'https://gradio-hugginface-aiserver-production.up.railway.app/large',
  // Optional: 'proxy' or 'direct' to influence runtime client behavior
  AI_MODE: 'proxy'
};
