[
	{
		"id": "logic_example_test_action_one",
		"name": "Combine Test Action One Result with Event Value",
		"description": "Reads the result from 'test_action_one' (data[0].total) and an event field 'detail.value', returns combined summary.",
		"inputs": [
			{
				"type": "action",
				"source": "Test Action One",
				"actionId": "test_action_one",
				"field": "data[0].total",
				"alias": "invoiceTotal"
			},
			{
				"type": "event",
				"source": "sample_event",
				"eventId": "sample_event",
				"field": "detail.value",
				"alias": "eventValue"
			}
		],
		"functionCode": "// inputs.invoiceTotal (number)\n// inputs.eventValue (number or string)\nconst total = (Number(inputs.invoiceTotal) || 0) + (Number(inputs.eventValue) || 0);\nreturn { total, invoice: inputs.invoiceTotal, eventValue: inputs.eventValue };",
		"updatedAt": "2025-12-24T00:00:00.000Z",
		"exampleContext": {
			"actions": {
				"test_action_one": [
					{ "total": 120.5 }
				]
			},
			"events": {
				"sample_event": {
					"detail": { "value": 30 }
				}
			}
		}
	}
]
